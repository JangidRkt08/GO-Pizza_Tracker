{{template "bottom" .}}

    <title>Order Status</title>
    </head>
    <body>
        <div class="mx-w-md mx-auto text-center">
            <h1 class="text-3xl font-bold mb-4">
                Order Placed succesfully
            </h1>
            <p class="text-lg mb-2">OrderID:</p>
            <p class="text-xl font-bold mb-4">{{.Order.ID}}</p>
        </div>
    </body>
{{template "bottom" .}}